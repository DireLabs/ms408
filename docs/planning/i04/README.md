# MS408 Research Program — Iteration 04 (focused: E10 confirm-or-kill)

**Mandate:** resolve the program's ONE live positive lead. i03/E10 reopened the
cross-organ **root↔leaf visual bundle** (overturning E4b's "annotation artifact"
verdict): it reproduces across two independent vision models and survives scribal-
hand + Currier-dialect stratification. But it is UNCONFIRMED, with two residual
confounds that i04 exists to close:

1. **Shared model lineage** — all three raters (Sonnet, Opus, Haiku) are Anthropic
   models with a common vision prior; a shared bias, not the manuscript, could drive
   the association. True independence needs a **non-Anthropic or human** rater.
2. **Illustration-style confound** — a drawing convention beyond scribal hand (how
   roots are coloured co-varying with how leaves are drawn) is not controlled.

i04 is deliberately SHORT and decisive: confirm the bundle (it becomes the program's
first graded referential-signal finding) or kill it (it returns to a style/lineage
artifact and the i01 "within-organ only" leg re-locks). The larger mid-level
linguistic program is **i05** (see L37; not in scope here).

**Inheritance.** All locks L1–L37; the standing clean-context refutation rule; L8
stratification. Continues the experiment ledger as **E11–E12**.

## Success criterion (i04)

A graded verdict on the root↔leaf bundle that survives a fresh refutation pass:
CONFIRMED (survives an independent-lineage rater AND a style control), KILLED
(collapses under either), or a documented "still unresolved, because …". L7 holds
throughout: this is a claim about *visual co-occurrence structure in the imagery*,
never about plaintext or a specific real taxon.

## Open decisions for Tim

- **D-i04-1 — independent-rater source (blocks E12).** The decisive test needs a
  rater OUTSIDE the Anthropic lineage. Options: (a) a **non-Anthropic vision model**
  (needs API credentials not currently in `.env` — e.g. an OpenAI/Google key); (b) a
  **human** rater pass on root_coloring + leaf_arrangement for the 129 herbal pages
  (logistics/time; the E4b critic's preferred ground truth); (c) defer E12 and let
  i04 rest on the style control (E11) alone. Tim's call on source + any spend.
- E11 (style control) is **unblocked** — pure code on existing annotations — and can
  run immediately.
