# Refutation — v5 walk-back (i09 / E25–E26 deflation), 2026-07-16

_Clean-context adversarial pass. Preserved from the session's adversary output._

## Verdict: directionally right, headline over-stated; contradicted by its own JSON in one place.

The defensible finding is that the i08 "gross incompatibility" was largely a slot-grammar
parameterisation artifact; freeing ED1 gets a positional/type-lexicon generator close on the
hard axes, with the obstruction *migrating* from ED1 to a coupled entropy/word-length wall.
That is a genuine deflation of the i08 negative, fairly graded **C**. Three specific claims are
**not supported as written**:

1. **"reproduces ALL hard axes to within ~0.03"** — false at the config level. **No single
   config** reproduces the full set (E25 `basin_all_core5:0.0`, ceiling 4/5; E26 `basin_all6:0.0`,
   `n_ge5:0`, ceiling 4/6). At the showcased config the residual is **two** misses — h2 off 0.034
   **and** mean word length ~6.38 vs band [4.92,5.13] (~1.25). "ED1 co-occurs with ΔI/TTR/Zipf"
   is a **cross-config composite** (a different config per pairing), never realised jointly.
2. **"the distributional signature does not discriminate the mechanism"** — over-broad and in
   tension with §4.6 (the signature *does* exclude the entire cipher-of-real-prose class). Narrow
   to "within this positional/type-lexicon family the hard axes under-determine the sub-mechanism."
3. **"fitting is easy / near-vacuous"** — contradicted by the **0% basin**: fitting was hard and
   incomplete, not vacuous.

Also: the scoring shifted from single-seed hard in/out (i08) to lenient multi-seed CI-overlap
(min..max touches band; ~9–15% of "matched" cells have their median out of band) and was **not**
applied back to the i08 families — not apples-to-apples. The mean-length "artifact" is an
untested conjecture and probably **not separable** (ED1-in-band configs are confined to the
smallest alphabet + widest spread — plausibly the same wall in a third coordinate). ED1 target
~0.75 is a 10k-subsample value vs 0.80 full.

## Honest middle (adopted): a *retraction* of i08's hard constraint, not "no constraint."

## Fixes applied → **v5b**
Dropped "reproduces all/near-vacuous/doesn't discriminate"; stated the ceilings numerically and
the two residuals; disclosed the scoring shift + soft axes + subsample target; re-asserted the
cross-class discrimination (cipher exclusion). Propagated to FLAGSHIP, TIMELINE, and the E26
verdict.
