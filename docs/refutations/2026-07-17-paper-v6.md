# Refutation — paper v6 (i06 cipher retraction), 2026-07-17

_Clean-context adversarial pass, briefed to check the retraction is correctly scoped and its
engagement with concurrent work (Greshko/Naibbe, Parisel) is accurate. Web-checked the external
papers._

## Verdict: retraction is the right call and its factual spine is sound, but the rewrite
over-claims in three places and contradicts the rest of the paper in two → **v6b warranted.**

## Confirmed sound (constrains the fixes)
Greshko genuinely respaces (respaced_plaintext mean token length 1.51 chars); word-boundary
Latin ΔI 0.076 is in the VMS band; homophony collapses ΔI with word order fixed; Greshko
concedes "incomplete replication of Voynich B"; Parisel (arXiv:2604.19762) is real.

## Disqualifying / must-fix
1. **[L1 firewall] "~30σ" is fabricated.** E31 reports **6.8σ (fc_local), 8.0σ (wc_local)** —
   "~30σ" appears in no result JSON, asserted 3× (abstract, §4.6, Discussion). A ~4× unsourced
   headline. → replace with 6.8/8.0σ.
2. **Over-correction: "homophonic viable" overstates "inconclusive".** E30: `homophonic_
   robustly_reaches_corner=false`; only **1 of 64** seed-configs hit 4/4; E31 non-separation is
   marginal (wc_local **1.2σ**). Honest reading: neither excluded nor robustly reached =
   inconclusive, not "viable." (Contradicts the paper's own Future Work hedge.) → downgrade.
3. **Hidden CI + grammar/drift contradiction.** E31 VMS wc_local 90% CI **[-1.44, 1.94]**
   crosses zero; paper reports only the 1.97 point as "real grammar." And it contradicts the
   E22 refutation (same corpus): "a block-wrapper control reproduces the VMS wc_z as sectional
   drift, not grammar." → report the CI; drop/qualify "real grammar"; reconcile with E22. (The
   exclusion survives — order-preserving at 6–16, far above the CI — but the sub-claim doesn't.)
4. **Two stale pre-retraction sentences** contradict §4.6: §"no family distinguished" still says
   "cipher of real prose is excluded"; §4.2 says §4.6 "closes" the abjad class ("closes" too
   strong; nomenclator wc_z 1.21 is near the VMS band; exclude on fc only). → fix both.

## Should-fix (fairness / accuracy)
5. **"~82%"** mixes corpora: word-boundary ΔI on book16 (9,243 tok) vs respaced/cipher on full
   nathist (34,764 tok); ΔI is scale-dependent. Direction right; qualify as text/spacing-specific.
6. **"Ran his ciphertext through our discriminators"** overstates: measures only our own
   ΔI/syntax axes, none of the glyph/word-structure properties Greshko claims; Naibbe h2 2.079 is
   itself out of the VMS band. Say we tested our axes, found ΔI uninformative.
7. **Parisel "converge"** is a stretch — Parisel argues *toward* cipher-like and *against* simple
   generators, while we keep a generator co-equal. → "adjacent/related, with a tension on the
   generator question." De-%UNVERIFIED (arXiv confirmed).
8. **Methodology boast** "before it reached the cipher's own author" is theatrical (nothing was
   sent). → "our own first-pass error, caught by the refutation pass before it was acted on."
9. ΔI leg is dead *as applied* (token-level); a block-scale like-for-like ΔI is untested. BLOCK=250
   local null is undefended (add a sensitivity note).

## Correct partition
"Word-order-preserving ciphers excluded (robust, 6.8–8.0σ, fc-driven) / verbose-homophonic
inconclusive (neither excluded nor robustly reached)." All fixes applied in **v6b**.
