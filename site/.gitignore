# Astro / node
node_modules/
dist/
.astro/
# Synced content is generated from ../docs at build time — not committed.
src/content/docs/
# Paper PDFs (site/public/papers/*.pdf) are committed as small release assets so the site
# deploys without a fragile LaTeX-in-CI step. Regenerate with scripts/build-papers.sh.
