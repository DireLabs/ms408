"""Phase 3 synthesis (T3.x): findings registry + competing narratives.

Firewall (L3): the registry pulls machine-computed numbers straight from
results/*.json — the synthesis never restates a statistic from memory.
"""
